/**
 * Класс базовых констант системы.
 * Аналог ru.spb.ipo.engine.utils.Constants
 */
export class Constants {
    // Пустая строка
    static EMPTY_STRING = "";

    // Замороженный пустой объект (аналог Collections.unmodifiableMap)
    static EMPTY_MAP = Object.freeze({});

    // Замороженный пустой массив
    static EMPTY_STRING_ARRAY = Object.freeze([]);
}
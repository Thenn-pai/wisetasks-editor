/**
 * Класс для работы с подстановками в множествах.
 */
export class SubstitutionSet {
    constructor() {
        this.substitutions = [];
    }

    // Базовая заглушка логики, которая обычно требуется для DistinctSet
    addSubstitution(sub) {
        this.substitutions.push(sub);
    }
}
/**
 * Компонент разделения экрана (Дерево слева / Свойства справа).
 * В JS обычно реализуется через CSS Flexbox или библиотеку split.js.
 *
 */
export class SmartSplitPane {
    constructor() {
        this.dividerLocation = 0.5;
    }
}